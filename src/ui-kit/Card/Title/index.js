// @flow
import * as RTCard from 'react-toolbox/lib/card';
import React, { PureComponent } from 'react';

export default class CardTitle extends PureComponent {
  render() {
    return <RTCard.CardTitle {...this.props} />;
  }
}
